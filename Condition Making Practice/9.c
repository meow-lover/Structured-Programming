//determining shortest distance to a destination

#include<stdio.h>

int main()
{
    float routeA = 15.6, routeB = 18;
    if(routeA > routeB)
    {
        printf("RouteB is the shortest distance to the destination");
    }
    else
    {
        printf("RouteA is the shortest distance to the destination");
    }

    return 0;
}
